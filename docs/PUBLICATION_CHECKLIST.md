# Publication Checklist

Before pushing this to a public GitHub repo:

- [x] Search for tokens, local keys, device IDs, home maps, and private IPs.
- [x] Replace private Cast UUID/host defaults with placeholders.
- [x] Make machine-specific paths configurable via environment variables or example placeholders.
- [x] Confirm `.env`, logs, audio files, caches, raw media dumps, and `~/.roborock` are not copied.
- [x] Run tests and library validation.
- [x] Add reviewed demo media only after visual/audio review.
- [x] Add creator/contact link: Andreas H LinkedIn.
- [ ] Decide public repo name, probably `dustlord` or `lord-dusk`.
- [ ] Create public GitHub remote and push.

## Suggested GitHub description

> Dust Lord: a local-agent Roborock persona that gives a robot vacuum memory, mood, and a dramatic Lord Dusk voice through TTS + Cast.

## Suggested topics

`robotics`, `smart-home`, `roborock`, `tts`, `google-cast`, `local-agents`, `hermes-agent`, `home-automation`, `python`, `macos`
